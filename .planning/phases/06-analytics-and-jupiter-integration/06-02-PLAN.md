---
phase: 06-analytics-and-jupiter-integration
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/web/app/dashboard/analytics/page.tsx
  - app/web/components/analytics/TShareChart.tsx
  - app/web/components/analytics/DistributionChart.tsx
  - app/web/components/analytics/SupplyChart.tsx
  - app/web/app/dashboard/layout.tsx
autonomous: true
must_haves:
  truths:
    - "User can view T-Share price chart on Analytics page"
    - "User can view Stake distribution histogram on Analytics page"
    - "User can view Token supply breakdown on Analytics page"
    - "Analytics link appears in sidebar"
  artifacts:
    - path: "app/web/app/dashboard/analytics/page.tsx"
      provides: "Main analytics view"
    - path: "app/web/components/analytics/TShareChart.tsx"
      provides: "Price history visualization"
  key_links:
    - from: "app/web/app/dashboard/analytics/page.tsx"
      to: "app/web/lib/api.ts"
      via: "React Query useQuery hook"
---

<objective>
Build the Analytics Dashboard components and integrate them into the frontend.

Purpose: Provide users with deep insights into protocol health and historical performance.
Output: Analytics page with multiple charts and updated navigation.
</objective>

<execution_context>
@/Users/annon/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/annon/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-analytics-and-jupiter-integration/06-RESEARCH.md
@app/web/app/dashboard/layout.tsx
@app/web/components/ui/chart.tsx
@app/web/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Analytics Chart Components</name>
  <files>app/web/components/analytics/TShareChart.tsx, app/web/components/analytics/DistributionChart.tsx, app/web/components/analytics/SupplyChart.tsx</files>
  <action>
    Create visualization components using `recharts`:
    
    1. `TShareChart.tsx`:
       - Line chart of `shareRate / 10000` over time.
       - Use `ChartWrapper` from `06-01`.
       - Add time period toggles (1d, 7d, 30d, all).
    
    2. `DistributionChart.tsx`:
       - Bar chart (histogram) of stake counts by duration bucket.
       - Data from `/api/stats/distribution/stakes`.
    
    3. `SupplyChart.tsx`:
       - Pie or Donut chart showing: Staked HELIX, Liquid Supply, Unclaimed Pool.
       - Calculate: Staked (from stats), Liquid (Supply - Staked), Unclaimed (Remaining BPD pool).
  </action>
  <verify>
    Check that components import `recharts` and use `ChartWrapper`.
  </verify>
  <done>Chart components created.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble Analytics Page and Update Navigation</name>
  <files>app/web/app/dashboard/analytics/page.tsx, app/web/app/dashboard/layout.tsx</files>
  <action>
    1. Create `app/web/app/dashboard/analytics/page.tsx`:
       - Main dashboard for analytics.
       - Use React Query `useQuery` to fetch data from the API client created in `06-01`.
       - Grid layout (2x2 or similar) for the charts.
       - Add section for "Protocol Stats" (daily inflation, total shares).
    
    2. Update `app/web/app/dashboard/layout.tsx`:
       - Add "Analytics" to `NAV_ITEMS`.
       - Label: "Analytics", href: "/dashboard/analytics", icon: BarChart3Icon (or custom SVG).
  </action>
  <verify>
    1. Verify `NAV_ITEMS` in `layout.tsx` contains Analytics.
    2. Verify `AnalyticsPage` uses `useQuery`.
  </verify>
  <done>Analytics page integrated and navigable.</done>
</task>

</tasks>

<verification>
Manual verify:
- Start dev server: `npm run dev` in `app/web`.
- Navigate to `/dashboard/analytics`.
- Confirm charts render (even if data is empty/mocked).
</verification>

<success_criteria>
- [ ] T-Share price history chart works.
- [ ] Stake distribution histogram works.
- [ ] Supply breakdown chart works.
- [ ] Sidebar has Analytics link.
</success_criteria>

<output>
After completion, create `.planning/phases/06-analytics-and-jupiter-integration/06-02-SUMMARY.md`
</output>
