---
phase: 06-analytics-and-jupiter-integration
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - app/web/app/dashboard/swap/page.tsx
  - app/web/components/swap/JupiterWidget.tsx
  - app/web/app/dashboard/layout.tsx
autonomous: false
must_haves:
  truths:
    - "User can view Swap page"
    - "Jupiter widget loads correctly"
    - "Wallet connection is passed to Jupiter widget"
    - "Swap link appears in sidebar"
  artifacts:
    - path: "app/web/components/swap/JupiterWidget.tsx"
      provides: "Jupiter Terminal integration"
  key_links:
    - from: "app/web/components/swap/JupiterWidget.tsx"
      to: "https://terminal.jup.ag/main-v4.js"
      via: "next/script"
---

<objective>
Integrate the Jupiter Swap widget into the dashboard.

Purpose: Allow users to easily acquire or exit HELIX positions without leaving the protocol.
Output: Swap page with embedded Jupiter Terminal.
</objective>

<execution_context>
@/Users/annon/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/annon/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-analytics-and-jupiter-integration/06-RESEARCH.md
@app/web/app/dashboard/layout.tsx
@app/web/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Jupiter Swap Widget</name>
  <files>app/web/components/swap/JupiterWidget.tsx, app/web/app/dashboard/swap/page.tsx</files>
  <action>
    1. Create `app/web/components/swap/JupiterWidget.tsx`:
       - Use `next/script` to load `https://terminal.jup.ag/main-v4.js`.
       - Implement a component that initializes `window.Jupiter.init` when the script is loaded.
       - Configuration:
         - `displayMode: 'integrated'`
         - `integratedTargetId: 'jupiter-terminal'`
         - `endpoint: process.env.NEXT_PUBLIC_RPC_URL`
         - `enableWalletPassthrough: true` (Critical for UX)
         - `strictTokenList: false` (to ensure HELIX is visible)
         - `initialOutputMint: HELIX_MINT_ADDRESS`
       - Handle wallet changes by updating the passthrough if necessary (see Jupiter docs).
    
    2. Create `app/web/app/dashboard/swap/page.tsx`:
       - Simple page container with an empty div `<div id="jupiter-terminal" />`.
       - Render the `JupiterWidget` component.
  </action>
  <verify>
    Verify script tag is added and initialization logic exists.
  </verify>
  <done>Jupiter widget component created and page scaffolded.</done>
</task>

<task type="auto">
  <name>Task 2: Update Sidebar Navigation</name>
  <files>app/web/app/dashboard/layout.tsx</files>
  <action>
    Add "Swap" to `NAV_ITEMS` in `layout.tsx`.
    Label: "Swap", href: "/dashboard/swap", icon: RefreshCwIcon (or custom SVG).
  </action>
  <verify>
    Verify `NAV_ITEMS` contains Swap.
  </verify>
  <done>Navigation updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Jupiter Swap Widget integration</what-built>
  <how-to-verify>
    1. Run the app: `npm run dev` in `app/web`.
    2. Navigate to `/dashboard/swap`.
    3. Confirm the Jupiter terminal loads.
    4. Confirm it shows your connected wallet (passthrough).
    5. Try selecting SOL -> HELIX (if mint is deployed).
  </how-to-verify>
  <resume-signal>Jupiter terminal visible and connected</resume-signal>
</task>

</tasks>

<verification>
Manual verify:
- Inspect `JupiterWidget.tsx` for `enableWalletPassthrough: true`.
- Confirm `NAV_ITEMS` contains Swap.
</verification>

<success_criteria>
- [ ] Jupiter script loads.
- [ ] Widget initializes in integrated mode.
- [ ] Wallet passthrough works.
- [ ] Swap link in sidebar.
</success_criteria>

<output>
After completion, create `.planning/phases/06-analytics-and-jupiter-integration/06-03-SUMMARY.md`
</output>
