---
phase: 07-leaderboard-and-marketing-site
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/web/app/dashboard/leaderboard/page.tsx
  - app/web/app/dashboard/whale-tracker/page.tsx
  - app/web/components/leaderboard/ranking-table.tsx
  - app/web/components/leaderboard/whale-feed.tsx
  - app/web/app/dashboard/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Leaderboard page displays top stakers ranked by active T-shares in a table with rank, wallet (truncated), T-shares, stakes count, and total staked"
    - "Current user's position is highlighted in the leaderboard if their wallet is connected"
    - "Whale tracker page shows a time-ordered feed of large stake/unstake events with type indicators"
    - "Both pages are accessible from the dashboard sidebar navigation"
  artifacts:
    - path: "app/web/app/dashboard/leaderboard/page.tsx"
      provides: "Leaderboard dashboard page"
      min_lines: 20
    - path: "app/web/app/dashboard/whale-tracker/page.tsx"
      provides: "Whale tracker dashboard page"
      min_lines: 20
    - path: "app/web/components/leaderboard/ranking-table.tsx"
      provides: "Reusable ranking table component with user highlighting"
      min_lines: 40
    - path: "app/web/components/leaderboard/whale-feed.tsx"
      provides: "Whale activity feed component"
      min_lines: 30
    - path: "app/web/app/dashboard/layout.tsx"
      provides: "Updated dashboard layout with leaderboard and whale tracker nav items"
  key_links:
    - from: "app/web/app/dashboard/leaderboard/page.tsx"
      to: "app/web/lib/api.ts"
      via: "React Query useQuery calling api.getLeaderboard"
      pattern: "api\\.getLeaderboard"
    - from: "app/web/app/dashboard/whale-tracker/page.tsx"
      to: "app/web/lib/api.ts"
      via: "React Query useQuery calling api.getWhaleActivity"
      pattern: "api\\.getWhaleActivity"
    - from: "app/web/app/dashboard/layout.tsx"
      to: "/dashboard/leaderboard, /dashboard/whale-tracker"
      via: "NAV_ITEMS array"
      pattern: "leaderboard|whale"
---

<objective>
Build the leaderboard and whale tracker dashboard pages with their supporting components, and add navigation entries to the dashboard sidebar.

Purpose: Users need to see competitive rankings and notable protocol activity. The leaderboard shows top stakers by T-shares, and the whale tracker shows large stake movements.

Output: Two new dashboard pages with React Query data fetching, reusable components, and updated sidebar navigation.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-leaderboard-and-marketing-site/07-01-SUMMARY.md
@app/web/app/dashboard/layout.tsx
@app/web/app/dashboard/analytics/page.tsx
@app/web/lib/api.ts
@app/web/lib/cn.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create leaderboard and whale tracker components and pages</name>
  <files>
    app/web/components/leaderboard/ranking-table.tsx
    app/web/components/leaderboard/whale-feed.tsx
    app/web/app/dashboard/leaderboard/page.tsx
    app/web/app/dashboard/whale-tracker/page.tsx
  </files>
  <action>
**ranking-table.tsx** - Client component ('use client'):
- Props: `data: LeaderboardEntry[]`, `currentUser: string | null`, `isLoading: boolean`
- Import `LeaderboardEntry` from `@/lib/api`, `Card` from `@/components/ui/card`, `Skeleton` from `@/components/ui/skeleton`, `cn` from `@/lib/cn`
- Loading state: render Skeleton with h-[600px]
- Table with columns: Rank (#), Wallet (truncated with font-mono, 8...6 chars), T-Shares (formatted: parseFloat / 1e9, toFixed(2)), Stakes (count), Total Staked (formatted: parseFloat / 1e9, toFixed(2) + " HELIX"), Duration (max_duration + " days")
- Current user row highlighted: bg-helix-600/10 border-l-2 border-helix-400, with "(You)" badge in text-helix-400
- Top 3 ranks get special styling: rank 1 = gold (#FFD700), rank 2 = silver (#C0C0C0), rank 3 = bronze (#CD7F32) text color on rank number
- Table header: border-b border-zinc-800, text-sm text-zinc-500, uppercase tracking-wider
- Table rows: border-b border-zinc-900, text-sm, hover:bg-zinc-900/50 transition-colors
- Wrap in Card with bg-surface border-zinc-800 p-0 overflow-hidden

**whale-feed.tsx** - Client component ('use client'):
- Props: `data: WhaleActivity[]`, `isLoading: boolean`
- Import `WhaleActivity` from `@/lib/api`, `Card` from `@/components/ui/card`, `Skeleton` from `@/components/ui/skeleton`, `cn` from `@/lib/cn`
- Loading state: render 5 Skeleton cards stacked vertically
- Each activity item rendered as a Card row with:
  - Left: Type badge - 'stake' = green bg (bg-emerald-500/10 text-emerald-400 "STAKED"), 'unstake' = red bg (bg-red-500/10 text-red-400 "UNSTAKED")
  - Middle: Wallet address (truncated, font-mono), amount formatted (parseFloat / 1e9, toFixed(2) + " HELIX")
  - Right: If stake type, show duration (days + " days") and T-shares; relative time from created_at using simple "X ago" calculation
- Items in a vertical stack with space-y-2

**leaderboard/page.tsx** - Client component ('use client'):
- Import useQuery from @tanstack/react-query, useWallet from @solana/wallet-adapter-react, api from @/lib/api
- Title: "Leaderboard" (text-3xl font-bold text-zinc-100), subtitle: "Top stakers ranked by active T-shares" (text-zinc-400)
- useQuery with key ['leaderboard', publicKey?.toBase58()], queryFn calling api.getLeaderboard(publicKey?.toBase58()), refetchInterval: 60000 (1 min)
- Render RankingTable component with data, currentUser=publicKey?.toBase58(), isLoading
- Show total participants count below subtitle if data available

**whale-tracker/page.tsx** - Client component ('use client'):
- Import useQuery, api
- Title: "Whale Tracker" (text-3xl font-bold text-zinc-100), subtitle: "Large stake movements" (text-zinc-400)
- useQuery with key ['whale-activity'], queryFn calling api.getWhaleActivity(50), refetchInterval: 30000 (30 sec)
- Render WhaleFeed component with data, isLoading
- Show threshold info text: "Showing transactions >= 100 HELIX" in text-xs text-zinc-500

Follow existing dashboard page patterns exactly - use same Tailwind color scheme (zinc-*, helix-*), same component patterns (Card, Skeleton), same query patterns.
  </action>
  <verify>
```bash
cd app/web && npx tsc --noEmit
```
All four files compile without errors.
  </verify>
  <done>
Leaderboard page renders ranked table with user highlight. Whale tracker page renders activity feed with type badges. Both use React Query for data fetching with appropriate refresh intervals.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add leaderboard and whale tracker to dashboard navigation</name>
  <files>app/web/app/dashboard/layout.tsx</files>
  <action>
Update the NAV_ITEMS array in `layout.tsx` to add two new navigation entries:

1. Add after the "Swap" entry:
   ```
   { label: "Leaderboard", href: "/dashboard/leaderboard", icon: TrophyIcon },
   { label: "Whale Tracker", href: "/dashboard/whale-tracker", icon: FishIcon },
   ```

2. Create TrophyIcon SVG component (same pattern as other icons in the file - inline SVG, className prop):
   - SVG path for a trophy/cup shape: circle at top, cup body, handles, base platform
   - Use simple path data: `<path d="M6 9H4.5a2.5 2.5 0 0 1 0-5H6" /><path d="M18 9h1.5a2.5 2.5 0 0 0 0-5H18" /><path d="M4 22h16" /><path d="M10 14.66V17c0 .55-.47.98-.97 1.21C7.85 18.75 7 20.24 7 22" /><path d="M14 14.66V17c0 .55.47.98.97 1.21C16.15 18.75 17 20.24 17 22" /><path d="M18 2H6v7a6 6 0 0 0 12 0V2Z" />`

3. Create FishIcon SVG component (whale/fish shape for whale tracker):
   - Use eye icon or wave-like shape: `<path d="M2 16s3-4 7-4 7 4 7 4" /><path d="M2 8s3 4 7 4 7-4 7-4" />` (activity/wave shape is better fit)
   - Actually use an Activity icon: `<path d="M22 12h-4l-3 9L9 3l-3 9H2" />`

Position the new nav items after "Swap" in the array to group analytics-related features together.
  </action>
  <verify>
```bash
cd app/web && npx tsc --noEmit
```
Navigation compiles. Verify NAV_ITEMS array has 8 entries total.
  </verify>
  <done>
Dashboard sidebar shows "Leaderboard" and "Whale Tracker" nav items with appropriate icons. Both link to their respective pages and highlight when active.
  </done>
</task>

</tasks>

<verification>
1. `cd app/web && npx tsc --noEmit` passes
2. Leaderboard page exists at `app/web/app/dashboard/leaderboard/page.tsx`
3. Whale tracker page exists at `app/web/app/dashboard/whale-tracker/page.tsx`
4. RankingTable component highlights current user's row
5. WhaleFeed component shows type badges (STAKED/UNSTAKED) with color coding
6. Dashboard layout NAV_ITEMS includes both new pages
7. Both pages use React Query with refetchInterval
</verification>

<success_criteria>
Both dashboard pages render with data from the indexer API. Leaderboard shows ranked stakers with the current wallet highlighted. Whale tracker shows a feed of large stake movements. Navigation sidebar includes both new pages with icons.
</success_criteria>

<output>
After completion, create `.planning/phases/07-leaderboard-and-marketing-site/07-02-SUMMARY.md`
</output>
