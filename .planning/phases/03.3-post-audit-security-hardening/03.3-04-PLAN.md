---
phase: 03.3-post-audit-security-hardening
plan: 04
type: execute
wave: 3
depends_on: ["03.3-02", "03.3-03"]
files_modified:
  - tests/bankrun/phase3/triggerBpd.test.ts
  - tests/bankrun/phase3.3/securityHardening.test.ts
autonomous: true

must_haves:
  truths:
    - "Existing BPD tests updated for new three-phase flow (finalize -> seal -> trigger) and pass"
    - "finalizeBpd helper passes remaining_accounts as writable (isWritable: true)"
    - "sealBpdFinalize helper function exists and is used in all BPD test flows"
    - "Security test: non-authority cannot call seal_bpd_finalize"
    - "Security test: duplicate stakes are skipped in finalize (bpd_finalize_period_id check)"
    - "Security test: trigger rejects stakes not counted in finalize"
    - "Security test: counter-based completion prevents premature completion"
    - "Security test: unstake blocked during BPD window"
    - "Security test: unstake includes bpd_bonus_pending in payout"
    - "Security test: claim_period_id=0 is rejected"
    - "All existing tests pass (regression)"
  artifacts:
    - path: "tests/bankrun/phase3/triggerBpd.test.ts"
      provides: "Updated existing BPD tests with seal step and writable accounts"
    - path: "tests/bankrun/phase3.3/securityHardening.test.ts"
      provides: "New security hardening tests"
  key_links:
    - from: "tests/bankrun/phase3.3/securityHardening.test.ts"
      to: "programs/helix-staking/src/instructions/seal_bpd_finalize.rs"
      via: "program.methods.sealBpdFinalize()"
      pattern: "sealBpdFinalize"
    - from: "tests/bankrun/phase3/triggerBpd.test.ts"
      to: "programs/helix-staking/src/instructions/seal_bpd_finalize.rs"
      via: "sealBpdFinalize helper call in test flow"
      pattern: "sealBpdFinalize"
---

<objective>
Update existing BPD tests for the new three-phase flow and add comprehensive security tests for all Phase 3.3 fixes.

Purpose: All code changes from Plans 01-03 must be verified. Existing tests need updating for the finalize->seal->trigger flow change (writable accounts, seal step). New tests must cover every security fix: CRIT-NEW-1 (rate manipulation), HIGH-1 (overflow), HIGH-2 (unstake window), MED-1 through MED-6, MED-8, and LOW-2.

Output: Full test coverage proving all vulnerabilities are fixed and no regressions introduced.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3-post-audit-security-hardening/CRIT-NEW-1-FIX-PLAN.md (Section 8: Test Scenarios)
@.planning/phases/03.3-post-audit-security-hardening/STATE-GUARD-FIX-PLAN.md (Section 13: Test Plan)
@.planning/phases/03.3-post-audit-security-hardening/03.3-01-SUMMARY.md
@.planning/phases/03.3-post-audit-security-hardening/03.3-02-SUMMARY.md
@.planning/phases/03.3-post-audit-security-hardening/03.3-03-SUMMARY.md

Source files to reference:
@tests/bankrun/phase3/triggerBpd.test.ts
@tests/bankrun/staking.test.ts (for test patterns and helpers)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing BPD tests for new three-phase flow</name>
  <files>tests/bankrun/phase3/triggerBpd.test.ts</files>
  <action>
    Read the existing `triggerBpd.test.ts` fully. Then make these changes:

    **1. Update finalizeBpd helper:**
    Find the helper function that calls `finalize_bpd_calculation`. Change the `remaining_accounts` to pass `isWritable: true` instead of `isWritable: false` for all stake accounts. The stakes must be writable because finalize now writes `bpd_finalize_period_id` to each stake.

    ```typescript
    // BEFORE (in remaining_accounts mapping):
    { pubkey: stakeKey, isSigner: false, isWritable: false }

    // AFTER:
    { pubkey: stakeKey, isSigner: false, isWritable: true }
    ```

    **2. Add sealBpdFinalize helper function:**
    Add a helper near the other BPD helpers:
    ```typescript
    async function sealBpdFinalize(
      context: BanksClient,
      program: Program,
      authority: Keypair,
      globalStatePDA: PublicKey,
      claimConfigPDA: PublicKey,
    ) {
      await program.methods
        .sealBpdFinalize()
        .accounts({
          authority: authority.publicKey,
          globalState: globalStatePDA,
          claimConfig: claimConfigPDA,
        })
        .signers([authority])
        .rpc();
    }
    ```

    NOTE: Adapt the helper to match the exact calling pattern used by the existing helpers in this test file (bankrun vs anchor provider, how RPC calls are made, etc.). Read the existing helpers carefully and follow the same style.

    **3. Insert seal step into all BPD test flows:**
    In EVERY test that calls `finalizeBpd` and then `triggerBpd`, insert a `sealBpdFinalize` call between them:
    ```
    // Old flow:
    await finalizeBpd(stakes);
    await triggerBpd(stakes);

    // New flow:
    await finalizeBpd(stakes);
    await sealBpdFinalize(authority, globalState, claimConfig);
    await triggerBpd(stakes);
    ```

    Go through every test case systematically. Common test patterns to update:
    - "distributes BPD proportionally"
    - "handles multiple batches"
    - "prevents duplicate BPD distribution"
    - "handles empty remaining_accounts"
    - Any test that exercises the full finalize->trigger flow

    **4. Update trigger completion assertions:**
    If any test checks for `big_pay_day_complete` becoming true based on batch size, update the assertion logic. Completion is now counter-based: it becomes true when `bpd_stakes_distributed >= bpd_stakes_finalized`, NOT when `remaining_accounts.len() < MAX`.

    **5. Run all existing tests:**
    ```bash
    cd /Users/annon/projects/solhex && npx jest tests/bankrun/phase3/triggerBpd.test.ts --no-cache 2>&1 | tail -40
    ```
    ALL existing tests must pass with the updated flow.
  </action>
  <verify>
    Run `cd /Users/annon/projects/solhex && npx jest tests/bankrun/phase3/triggerBpd.test.ts --no-cache 2>&1 | tail -40`. All tests pass. Also run the full test suite: `npx jest tests/bankrun/ --no-cache 2>&1 | tail -20` to check for regressions.
  </verify>
  <done>
    All existing BPD tests updated with writable remaining_accounts, seal step inserted between finalize and trigger, and all tests pass. No regressions in the full test suite.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 3.3 security hardening tests</name>
  <files>tests/bankrun/phase3.3/securityHardening.test.ts</files>
  <action>
    Create a new test file at `tests/bankrun/phase3.3/securityHardening.test.ts`.

    Follow the exact same test infrastructure patterns from the existing test files (imports, bankrun setup, helper functions, assertion patterns). Read `tests/bankrun/phase3/triggerBpd.test.ts` and `tests/bankrun/staking.test.ts` for the test template.

    **Test groups to implement:**

    **Group 1: CRIT-NEW-1 -- seal_bpd_finalize security**
    ```
    it("seal_bpd_finalize rejects non-authority signer")
      - Setup: Create stakes, finalize them
      - Action: Non-authority wallet calls sealBpdFinalize
      - Assert: Transaction fails with Unauthorized error

    it("seal_bpd_finalize rejects if no stakes finalized")
      - Setup: End claim period, do NOT call finalize
      - Action: Authority calls sealBpdFinalize
      - Assert: Fails with NoEligibleStakers

    it("seal_bpd_finalize rejects double seal")
      - Setup: Finalize + seal once
      - Action: Authority calls sealBpdFinalize again
      - Assert: Fails with BpdCalculationAlreadyComplete

    it("finalize rejects calls after seal")
      - Setup: Finalize + seal
      - Action: Call finalizeBpd with more stakes
      - Assert: Fails with BpdCalculationAlreadyComplete
    ```

    **Group 2: CRIT-NEW-1 -- per-stake finalize tracking**
    ```
    it("duplicate stake in finalize is skipped silently")
      - Setup: 1 stake
      - Action: Finalize batch 1 with [stake_A], finalize batch 2 with [stake_A] again
      - Assert: bpd_stakes_finalized == 1, bpd_total_share_days unchanged after batch 2

    it("trigger skips stakes not counted in finalize")
      - Setup: 2 stakes (A, B), only finalize stake_A
      - Action: Seal, then trigger with [stake_A, stake_B]
      - Assert: Only stake_A receives BPD, stake_B skipped
    ```

    **Group 3: CRIT-NEW-1 -- counter-based completion**
    ```
    it("trigger does not mark complete until all finalized stakes distributed")
      - Setup: 3 stakes finalized
      - Action: Trigger batch 1 with [stake_A] (1 of 3)
      - Assert: big_pay_day_complete == false
      - Action: Trigger batch 2 with [stake_B, stake_C]
      - Assert: big_pay_day_complete == true
    ```

    **Group 4: HIGH-2 -- BPD window guard**
    ```
    it("unstake blocked during BPD window")
      - Setup: Create stake, end claim period, call finalize (sets BPD window)
      - Action: Try to unstake
      - Assert: Fails with UnstakeBlockedDuringBpd

    it("unstake allowed after BPD window closes")
      - Setup: Full BPD flow (finalize + seal + trigger) completes
      - Action: Try to unstake
      - Assert: Success
    ```

    **Group 5: MED-5 -- claim_period_id validation**
    ```
    it("initialize_claim_period rejects claim_period_id=0")
      - Action: Call initialize_claim_period with claim_period_id=0
      - Assert: Fails with InvalidClaimPeriodId

    it("initialize_claim_period accepts claim_period_id=1")
      - Action: Call initialize_claim_period with claim_period_id=1
      - Assert: Success
    ```

    **Group 6: LOW-2 -- BPD bonus in unstake**
    ```
    it("unstake includes bpd_bonus_pending in payout")
      - Setup: Create stake, run BPD (finalize + seal + trigger) to give it a bonus
      - Action: Unstake (without calling claim_rewards first)
      - Assert: Payout includes the BPD bonus amount
      - Assert: stake.bpd_bonus_pending == 0 after unstake
    ```

    **Group 7: Integration -- full hardened lifecycle**
    ```
    it("full BPD lifecycle with security hardening")
      - Setup: Initialize claim period, create 3 stakes, advance past end
      - Action: Finalize all 3 across 2 batches, seal, trigger across 2 batches
      - Assert: All 3 receive proportional BPD, big_pay_day_complete == true
      - Assert: Total distributed <= total unclaimed
    ```

    IMPORTANT IMPLEMENTATION NOTES:
    - Use the EXACT same test infrastructure as existing tests (bankrun client setup, program loading, etc.)
    - For error assertion, use the pattern from existing tests (try/catch with error message matching, or `.should.be.rejectedWith`)
    - Create helper functions for common setup (initialize + create stakes + end claim period + finalize)
    - Account balances should be checked using SPL token account reads
    - Slot advancement should use the bankrun clock warp function used in existing tests
    - Keep tests focused -- each test should verify ONE specific security property
  </action>
  <verify>
    Run `cd /Users/annon/projects/solhex && npx jest tests/bankrun/phase3.3/securityHardening.test.ts --no-cache 2>&1 | tail -40`. All new security tests pass.

    Then run full suite: `cd /Users/annon/projects/solhex && npx jest tests/bankrun/ --no-cache 2>&1 | tail -20`. All tests pass (no regressions).
  </verify>
  <done>
    Security hardening test suite exists with tests covering CRIT-NEW-1 (seal security, per-stake tracking, counter completion), HIGH-2 (BPD window guard), MED-5 (claim period id validation), LOW-2 (bonus in unstake), and full lifecycle integration. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. `npx jest tests/bankrun/phase3/triggerBpd.test.ts` -- ALL existing BPD tests pass with new flow
2. `npx jest tests/bankrun/phase3.3/securityHardening.test.ts` -- ALL new security tests pass
3. `npx jest tests/bankrun/` -- Full test suite passes with zero failures
4. Security tests cover: seal auth, seal state guards, duplicate prevention, finalize tracking, counter completion, BPD window, claim period id, BPD bonus in unstake, full lifecycle
</verification>

<success_criteria>
All existing tests pass after flow update. All new security tests pass. Total test coverage proves every vulnerability (CRIT-NEW-1, HIGH-1, HIGH-2, MED-1 through MED-6, MED-8, LOW-2) is fixed. No regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-post-audit-security-hardening/03.3-04-SUMMARY.md`
</output>
