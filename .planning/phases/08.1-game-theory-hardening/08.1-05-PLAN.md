---
phase: 08.1-game-theory-hardening
plan: 05
type: execute
wave: 4
depends_on: ["08.1-02", "08.1-03", "08.1-04"]
files_modified:
  - tests/bankrun/phase8.1/gameTheory.test.ts
  - tests/bankrun/phase8.1/auditFixes.test.ts
autonomous: true

must_haves:
  truths:
    - "Loyalty multiplier tests: 0% at day 0, ~25% at half-term, 50% at full term, capped past term"
    - "Loyalty multiplier applies to claim_rewards and unstake auto-claim"
    - "BPD whale cap test: stake with >5% share-days gets capped bonus"
    - "BPD whale cap redistributes excess to remaining stakes"
    - "BPB diminishing returns: amounts ≤ 150M unchanged, 500M gets reduced bonus, 1B+ hits cap"
    - "abort_bpd reset test: after abort, re-initialize + finalize works correctly"
    - "slots_per_day=0 rejected in initialize"
    - "seal_bpd_finalize rejected before 24h delay"
    - "seal_bpd_finalize succeeds after 24h delay"
    - "BpdBatchFinalized events emitted with correct data"
    - "All 94+ existing tests still pass"
  artifacts:
    - path: "tests/bankrun/phase8.1/gameTheory.test.ts"
      provides: "Game theory mechanics tests"
      contains: "loyalty"
    - path: "tests/bankrun/phase8.1/auditFixes.test.ts"
      provides: "Audit fix verification tests"
      contains: "abort_bpd"
  key_links:
    - from: "tests/bankrun/phase8.1/gameTheory.test.ts"
      to: "programs/helix-staking/src/instructions/claim_rewards.rs"
      via: "bankrun instruction calls"
      pattern: "claim_rewards"
    - from: "tests/bankrun/phase8.1/gameTheory.test.ts"
      to: "programs/helix-staking/src/instructions/trigger_big_pay_day.rs"
      via: "bankrun instruction calls"
      pattern: "trigger_big_pay_day"
---

<objective>
Write comprehensive Bankrun tests covering all Phase 8.1 mechanics: duration loyalty multiplier, anti-whale BPD cap, BPB diminishing returns, audit fixes, and BPD transparency/delay.

Purpose: Every new economic mechanic and security fix needs test coverage to prove correctness and provide regression protection.
Output: 20+ new tests covering all Phase 8.1 success criteria.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-game-theory-hardening/08.1-RESEARCH.md

# Test utilities and patterns
@tests/bankrun/utils.ts
@tests/bankrun/phase3/utils.ts
@tests/bankrun/phase3.3/securityHardening.test.ts
@tests/bankrun/phase3.3/securityFixes.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create game theory test suite</name>
  <files>tests/bankrun/phase8.1/gameTheory.test.ts</files>
  <action>
    Create `tests/bankrun/phase8.1/gameTheory.test.ts` with vitest syntax. Follow existing bankrun test patterns.

    **Duration Loyalty Multiplier Tests:**

    1. "freshly created stake gets 0% loyalty bonus on claim":
       - Create a 365-day stake
       - Advance 1 day, run crank
       - Claim rewards, verify amount matches base rewards (no loyalty boost)

    2. "half-term stake gets ~25% loyalty bonus on claim":
       - Create a 365-day stake
       - Advance 182 days, run cranks
       - Claim rewards, verify amount is ~25% higher than pure share_rate calculation

    3. "full-term stake gets 50% loyalty bonus on claim":
       - Create a 365-day stake
       - Advance 365 days, run cranks
       - Claim rewards at maturity, verify 50% bonus applied

    4. "loyalty bonus does not exceed max in grace period":
       - Create a 365-day stake
       - Advance 375 days (10 into grace period), run cranks
       - Claim rewards, verify bonus is still capped at 50%

    5. "loyalty bonus applied to auto-claimed rewards during unstake":
       - Create a 365-day stake, advance 365 days
       - Unstake (on-time), verify payout includes loyalty-boosted rewards

    6. "loyalty bonus scales linearly between 0 and max":
       - Create a 1000-day stake
       - Claim at day 100, day 500, day 1000
       - Verify bonuses are approximately 5%, 25%, 50% respectively

    **Anti-Whale: BPD Share Cap Tests:**

    7. "whale stake capped at 5% of BPD pool":
       - Create one whale stake (90% share-days) and several small stakes
       - Run full BPD flow
       - Verify whale's BPD ≤ 5% of total unclaimed pool
       - Verify remaining unclaimed stays in pool

    8. "small stakes below cap receive full calculated BPD":
       - Create multiple equal-sized stakes, each well below 5%
       - Run full BPD flow
       - Verify each receives their full calculated share

    9. "excess from capped stakes redistributed":
       - Create 1 whale (would take 40%) + 20 small stakes
       - Run BPD flow
       - Verify small stakes collectively receive more than they would without the cap

    **Anti-Whale: BPB Diminishing Returns Tests:**

    10. "BPB unchanged for stakes ≤ 150M":
        - Create stakes at 0, 50M, 100M, 150M amounts
        - Verify T-shares match original linear formula exactly

    11. "BPB diminishing returns above 150M":
        - Create stake at 300M (mid tier 2)
        - Verify BPB bonus < what linear extrapolation would give

    12. "BPB hard capped at BPB_MAX_BONUS above tier 3":
        - Create stake at 2B (above tier 3)
        - Verify BPB bonus == BPB_MAX_BONUS (1.5x)

    13. "BPB tiers produce monotonically increasing bonus":
        - Create stakes at 150M, 300M, 500M, 750M, 1B, 2B
        - Verify each gets a higher T-share count than the previous
        - Verify the increments decrease (diminishing returns)
  </action>
</task>

<task type="auto">
  <name>Task 2: Create audit fix and transparency test suite</name>
  <files>tests/bankrun/phase8.1/auditFixes.test.ts</files>
  <action>
    Create `tests/bankrun/phase8.1/auditFixes.test.ts` with vitest syntax.

    **Audit Fix Tests:**

    14. "abort_bpd resets bpd_remaining_unclaimed (HIGH-NEW-1)":
        - Start BPD finalization
        - Call abort_bpd
        - Fetch ClaimConfig, verify bpd_remaining_unclaimed == 0
        - Verify bpd_total_share_days == 0

    15. "after abort, new claim period can finalize + distribute":
        - Full BPD → abort → initialize new claim period → finalize → seal → trigger
        - Verify distribution completes successfully

    16. "initialize rejects slots_per_day=0 (XRAY-3)":
        - Attempt initialize with slots_per_day=0
        - Verify it fails with InvalidParameter error

    17. "calculate_days_elapsed handles zero safely":
        - This is validated implicitly through the initialize rejection test
        - Create normal program and verify days calculation works with valid slots_per_day

    **BPD Transparency Tests:**

    18. "BpdBatchFinalized event emitted with correct data":
        - Run finalize_bpd_calculation
        - Parse transaction logs for BpdBatchFinalized event
        - Verify claim_period_id, batch_stakes_processed, total_stakes_finalized fields

    19. "seal_bpd_finalize rejects before delay period":
        - Run finalize batches
        - Immediately attempt seal
        - Verify it fails with BpdSealTooEarly

    20. "seal_bpd_finalize succeeds after delay period":
        - Run finalize batches
        - Advance clock by BPD_SEAL_DELAY_SECONDS + 1
        - Seal succeeds

    21. "bpd_finalize_start_timestamp set on first batch only":
        - Run finalize batch 1, note timestamp
        - Advance clock, run finalize batch 2
        - Verify timestamp unchanged (still equals batch 1 time)

    **Regression:**

    22. "all 94+ existing tests still pass":
        - This task's verification step: run full test suite
  </action>
</task>

<task type="auto">
  <name>Task 3: Run full test suite</name>
  <files>tests/bankrun/**</files>
  <action>
    Run `vitest run tests/bankrun` to verify:
    1. All new Phase 8.1 tests pass
    2. All existing tests still pass (some may need clock advancement for seal delay)
    
    If existing BPD tests fail due to the new seal delay, add clock advancement before seal calls.
  </action>
</task>

</tasks>
