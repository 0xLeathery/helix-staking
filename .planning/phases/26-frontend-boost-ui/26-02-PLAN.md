---
phase: 26-frontend-boost-ui
plan: "02"
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - app/web/lib/hooks/useRegisterBoost.ts
  - app/web/lib/solana/compute-budget.ts
  - app/web/__tests__/hooks/useRegisterBoost.test.ts
  - app/web/components/dashboard/boost-status-card.tsx
  - app/web/app/dashboard/page.tsx
  - app/web/__tests__/components/boost-status-card.test.tsx
autonomous: true
requirements: [FRONT-03]

must_haves:
  truths:
    - "A user with seed tokens sees a 'Register Boost' button on the dashboard"
    - "Clicking 'Register Boost' calls register_seed_boost on-chain via connected wallet"
    - "After successful registration the UI updates to show 'Boost registered' state"
    - "A user who already has a BoostRecord sees appropriate status (registered, active, or revoked) not the register button"
    - "A user without seed tokens sees no boost registration prompt"
  artifacts:
    - path: "app/web/lib/hooks/useRegisterBoost.ts"
      provides: "useMutation hook calling register_seed_boost instruction"
      exports: ["useRegisterBoost"]
    - path: "app/web/__tests__/hooks/useRegisterBoost.test.ts"
      provides: "Behavioral tests for useRegisterBoost mutation hook"
      contains: "registerSeedBoost"
    - path: "app/web/components/dashboard/boost-status-card.tsx"
      provides: "Dashboard-level boost status card with register button and status display"
      exports: ["BoostStatusCard"]
    - path: "app/web/__tests__/components/boost-status-card.test.tsx"
      provides: "Unit tests for BoostStatusCard render states"
      contains: "BoostStatusCard"
    - path: "app/web/app/dashboard/page.tsx"
      provides: "Dashboard page with BoostStatusCard integrated"
      contains: "BoostStatusCard"
  key_links:
    - from: "app/web/components/dashboard/boost-status-card.tsx"
      to: "app/web/lib/hooks/useRegisterBoost.ts"
      via: "useRegisterBoost mutation hook"
      pattern: "useRegisterBoost"
    - from: "app/web/components/dashboard/boost-status-card.tsx"
      to: "app/web/lib/hooks/useSeedBalance.ts"
      via: "useSeedBalance for eligibility check"
      pattern: "useSeedBalance"
    - from: "app/web/components/dashboard/boost-status-card.tsx"
      to: "app/web/lib/hooks/useBoostRecord.ts"
      via: "useBoostRecord for registration status"
      pattern: "useBoostRecord"
    - from: "app/web/lib/hooks/useRegisterBoost.ts"
      to: "register_seed_boost on-chain instruction"
      via: "program.methods.registerSeedBoost()"
      pattern: "registerSeedBoost"
---

<objective>
Create the useRegisterBoost mutation hook and BoostStatusCard dashboard component so users can register for the APY boost directly from the dashboard.

Purpose: FRONT-03 requires a wallet interaction to call `register_seed_boost` on-chain. The BoostStatusCard provides the dashboard-level UI for boost status and the register button. This plan depends on Plan 01 (IDL sync, types, hooks) being complete.

Output: useRegisterBoost mutation hook with behavioral tests, BoostStatusCard component with render-state tests, dashboard page integration.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-frontend-boost-ui/26-RESEARCH.md
@.planning/phases/26-frontend-boost-ui/26-01-SUMMARY.md

@app/web/lib/hooks/useCreateStake.ts
@app/web/lib/solana/compute-budget.ts
@app/web/app/dashboard/page.tsx
@app/web/__tests__/hooks/useTransactionSimulation.test.ts

<interfaces>
<!-- Key types and contracts from Plan 01 output (executor should verify after Plan 01 completes) -->

From app/web/lib/solana/pdas.ts (after Plan 01):
```typescript
export function deriveBoostRecord(user: PublicKey): [PublicKey, number];
export function deriveGlobalState(): [PublicKey, number];
```

From app/web/lib/hooks/useSeedBalance.ts (created in Plan 01):
```typescript
export function useSeedBalance(): UseQueryResult<BN>;
export function getSeedMintFromGlobalState(globalState: any): PublicKey;
```

From app/web/lib/hooks/useBoostRecord.ts (created in Plan 01):
```typescript
export function useBoostRecord(): UseQueryResult<BoostRecordData | null>;
```

From app/web/components/stake/boost-badge.tsx (created in Plan 01):
```typescript
export type BoostState = 'none' | 'eligible' | 'active' | 'revoked';
export function BoostBadge({ state }: { state: BoostState }): JSX.Element | null;
export function getBoostState(account: {...}): BoostState;
```

From app/web/lib/hooks/useCreateStake.ts (existing pattern to mirror):
```typescript
// Uses: useProgram(), useWallet(), useConnection(), useMutation()
// Pattern: build tx -> simulate -> sendTransaction -> confirm -> invalidate queries
```

From app/web/lib/solana/compute-budget.ts (existing):
```typescript
export function getComputeBudgetInstructions(units: number): TransactionInstruction[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useRegisterBoost mutation hook with behavioral tests</name>
  <files>
    app/web/lib/hooks/useRegisterBoost.ts,
    app/web/lib/solana/compute-budget.ts,
    app/web/__tests__/hooks/useRegisterBoost.test.ts
  </files>
  <behavior>
    - mutationFn calls program.methods.registerSeedBoost() with correct accounts (globalState, boostRecord, seedTokenAccount, seedMint, seedTokenProgram, associatedTokenProgram)
    - mutationFn simulates the transaction before calling sendTransaction (security requirement)
    - mutationFn throws "Wallet not connected" when publicKey is null
    - mutationFn throws "Seed mint not configured" when seed mint equals PublicKey.default
    - onSuccess invalidates ["boostRecord", pubkey], ["stakes", pubkey], and ["seedBalance", pubkey] query caches
    - Seed ATA is derived with standard SPL Token program ID (not Token-2022)
  </behavior>
  <action>
    1. Write tests FIRST in `app/web/__tests__/hooks/useRegisterBoost.test.ts`:
       - This tests the mutation function directly (not via renderHook — the mutation function is the unit under test)
       - Mock approach (mirrors useTransactionSimulation.test.ts pattern — vi.mock at module level, plain function tests):
         - vi.mock `@/lib/hooks/useProgram` to return a mock program object with `program.account.globalState.fetch` and `program.methods.registerSeedBoost().accountsPartial().transaction()`
         - vi.mock `@solana/wallet-adapter-react` for `useWallet` and `useConnection`
         - vi.mock `@tanstack/react-query` for `useMutation` and `useQueryClient`
         - vi.mock `@/lib/solana/pdas` for `deriveGlobalState` and `deriveBoostRecord`
         - vi.mock `@/lib/solana/compute-budget` for `getComputeBudgetInstructions`
         - vi.mock `@/lib/hooks/useSeedBalance` for `getSeedMintFromGlobalState`
       - ALTERNATIVE simpler approach (PREFERRED): Since useRegisterBoost calls useMutation and passes a mutationFn, extract the core transaction-building logic into a testable pure function `buildRegisterBoostTx(params)` that the hook calls. Test that function directly without React hooks. This mirrors how useTransactionSimulation.test.ts tests `simulateTransactionOrThrow` as a standalone function.
       - Test cases:
         a. `throws "Wallet not connected" when publicKey is null` — call with null publicKey, expect error
         b. `throws "Seed mint not configured" when seed mint is default` — mock getSeedMintFromGlobalState to return PublicKey.default, expect error
         c. `calls registerSeedBoost with correct accounts` — mock all dependencies, verify program.methods.registerSeedBoost() is called and accountsPartial receives the correct account keys
         d. `simulates transaction before sending` — verify connection.simulateTransaction is called before sendTransaction
         e. `uses standard SPL Token program ID for seed ATA` — verify getAssociatedTokenAddressSync is called with TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA (not Token-2022)
       - Run tests: expect RED (useRegisterBoost.ts doesn't exist yet)

    2. Create `app/web/lib/hooks/useRegisterBoost.ts` mirroring the `useCreateStake.ts` mutation pattern exactly:
       - Import: `useConnection`, `useWallet` from wallet-adapter-react; `useMutation`, `useQueryClient` from tanstack/react-query; `getAssociatedTokenAddressSync` from spl-token; `useProgram` from `./useProgram`; `deriveGlobalState`, `deriveBoostRecord` from `@/lib/solana/pdas`; `getComputeBudgetInstructions` from `@/lib/solana/compute-budget`; `getSeedMintFromGlobalState` from `./useSeedBalance`
       - Define constants for SPL Token program ID: `new PublicKey("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA")` and Associated Token Program ID: `new PublicKey("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL")`
       - Use standard SPL Token program for seed ATA derivation (NOT Token-2022 -- pump.fun tokens use standard SPL per research)
       - Export the core transaction logic as a testable function if needed for the test approach
       - `useMutation<{ signature: string }, Error, void>` with mutationFn that:
         a. Checks `publicKey` exists, throws "Wallet not connected" if not
         b. Fetches `globalState` via `program.account.globalState.fetch(globalStatePda)`
         c. Extracts `seedMint` via `getSeedMintFromGlobalState(globalState)` — throws "Seed mint not configured" if `PublicKey.default`
         d. Derives `boostRecordPda` via `deriveBoostRecord(publicKey)`
         e. Derives `seedTokenAccount` via `getAssociatedTokenAddressSync(seedMint, publicKey, false, SPL_TOKEN_PROGRAM_ID, ASSOCIATED_TOKEN_PROGRAM_ID)`
         f. Builds transaction: `program.methods.registerSeedBoost().accountsPartial({ user: publicKey, globalState: globalStatePda, boostRecord: boostRecordPda, seedTokenAccount, seedMint, seedTokenProgram: SPL_TOKEN_PROGRAM_ID, associatedTokenProgram: ASSOCIATED_TOKEN_PROGRAM_ID }).transaction()`
         g. Prepends compute budget instructions: `getComputeBudgetInstructions(150_000)` — PDA init requires ~150K CU
         h. Sets `recentBlockhash` and `feePayer`
         i. Simulates transaction first (security requirement — all existing hooks simulate before send)
         j. Calls `sendTransaction(tx, connection)` then `confirmTransaction`
         k. Returns `{ signature }`
       - `onSuccess`: invalidate `["boostRecord", publicKey]`, `["stakes", publicKey]`, `["seedBalance", publicKey]` query caches
       - `onError`: optionally log for debugging, but let React Query surface the error
       - Run tests: expect GREEN

    3. Add CU limit constant to `app/web/lib/solana/compute-budget.ts`:
       If the file has a CU_LIMITS object, add `registerSeedBoost: 150_000` to it. If not, just note that 150_000 is used directly in the hook.
  </action>
  <verify>
    <automated>cd /Users/annon/projects/solhex/app/web && npx vitest run __tests__/hooks/useRegisterBoost.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - useRegisterBoost hook exists and TypeScript compiles without errors
    - Behavioral tests verify: registerSeedBoost is called with correct accounts, simulation runs before send, wallet-not-connected error thrown, seed-mint-not-configured error thrown, standard SPL Token program used for ATA
    - Hook uses standard SPL Token program for seed ATA (not Token-2022)
    - Transaction simulation happens before send (security pattern)
    - Query caches are invalidated on success
    - All useRegisterBoost tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BoostStatusCard component with render-state tests and dashboard integration</name>
  <files>
    app/web/components/dashboard/boost-status-card.tsx,
    app/web/app/dashboard/page.tsx,
    app/web/__tests__/components/boost-status-card.test.tsx
  </files>
  <behavior>
    - BoostStatusCard returns null (no render) when wallet is not connected
    - BoostStatusCard returns null when user has no seed balance AND no BoostRecord
    - BoostStatusCard renders "Eligible" state with Register button when user has seed balance but no BoostRecord
    - BoostStatusCard renders "Registered" state when user has BoostRecord with boostedStakeId === u64::MAX
    - BoostStatusCard renders "Active" state with green indicator when BoostRecord has real boostedStakeId
    - BoostStatusCard renders "Revoked" state when boost is revoked
    - Register button is disabled while mutation is pending
  </behavior>
  <action>
    1. Write tests FIRST in `app/web/__tests__/components/boost-status-card.test.tsx`:
       - Mock approach (mirrors protocol-paused-banner.test.tsx pattern — render + screen queries):
         - vi.mock `@/lib/hooks/useSeedBalance` returning configurable `{ data: BN }` per test
         - vi.mock `@/lib/hooks/useBoostRecord` returning configurable `{ data: BoostRecordData | null }` per test
         - vi.mock `@/lib/hooks/useRegisterBoost` returning `{ mutate, isPending, error }` per test
         - vi.mock `@solana/wallet-adapter-react` for `useWallet` returning `{ publicKey }` per test
         - vi.mock `@/lib/hooks/useGlobalState` returning `{ data: { reserved: [...] } }` with boost enabled
       - Test cases:
         a. `renders nothing when wallet not connected` — set publicKey to null, expect container.firstChild to be null
         b. `renders nothing when no seed balance and no boost record` — set seedBalance to BN(0), boostRecord to null, expect container.firstChild to be null
         c. `renders eligible state with Register button when has seed balance, no boost record` — set seedBalance to BN(1000), boostRecord to null, expect "Register Boost" button visible
         d. `renders registered state when boost record has u64::MAX stakeId` — set boostRecord with boostedStakeId = BN("18446744073709551615"), expect "Registered" text or similar
         e. `renders active state when boost record has real stakeId` — set boostRecord with boostedStakeId = BN(5), expect green/active indicator
         f. `renders revoked state` — configure to show revoked state, expect "Revoked" text
         g. `Register button disabled while mutation pending` — set isPending to true, expect button disabled
       - Run tests: expect RED (component doesn't exist yet)

    2. Create `app/web/components/dashboard/boost-status-card.tsx`:
       - Import hooks: `useSeedBalance`, `useBoostRecord`, `useRegisterBoost` from `@/lib/hooks/*`
       - Import: `useWallet` from wallet-adapter-react, `useGlobalState` from hooks, `useStakes` from hooks (if available, to check if any stake is boosted)
       - Import UI: `Card`, `CardContent`, `CardHeader`, `CardTitle` from `@/components/ui/card`, `Button` from `@/components/ui/button`, `BoostBadge` from `@/components/stake/boost-badge`
       - Import: `Zap` icon from lucide-react, `BN` from bn.js
       - Component logic — derive dashboard-level boost state:
         a. If wallet not connected: return null (no card)
         b. Check `useGlobalState()` — if boost is not enabled (reserved[7] is zero or globalState missing): return null
         c. `useSeedBalance()` — get seed token balance
         d. `useBoostRecord()` — get BoostRecord PDA (null = not registered)
         e. `useRegisterBoost()` — get mutation for register button
         f. Determine dashboard state:
            - No seed balance AND no boost record: return null (user has nothing to do with boost)
            - Has seed balance, no boost record: show "Eligible" state with Register button
            - Has boost record, boostedStakeId === u64::MAX: show "Registered -- create a stake to activate"
            - Has boost record, boostedStakeId !== u64::MAX, not revoked: show "Active" state with green indicator
            - Boost record with any stake showing boostRevoked: show "Revoked" state
       - Register button: disabled while mutation is pending, shows loading spinner
       - Error state: if mutation errors, show error message below button
       - Card styling: match existing dashboard card patterns (zinc-900 bg, zinc-100 text)
       - Add Framer Motion `m.div` with `variants={staggerItem}` for consistency with dashboard layout
       - Run tests: expect GREEN

    3. Modify `app/web/app/dashboard/page.tsx`:
       - Import `BoostStatusCard` from `@/components/dashboard/boost-status-card`
       - Add BoostStatusCard after the ProtocolPausedBanner and before ProtocolStats:
         ```tsx
         {/* Boost Status */}
         <m.div variants={staggerItem}>
           <ErrorBoundary>
             <BoostStatusCard />
           </ErrorBoundary>
         </m.div>
         ```
       - Wrap in ErrorBoundary for safety (boost card failure should not crash dashboard)

    NOTE: The u64::MAX sentinel value for boostedStakeId is `new BN("18446744073709551615")`. Compare with `.eq()` method. When boostedStakeId equals this value, the BoostRecord exists but no stake has been linked yet.
  </action>
  <verify>
    <automated>cd /Users/annon/projects/solhex/app/web && npx vitest run __tests__/components/boost-status-card.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - BoostStatusCard renders on dashboard when user has seed tokens or an existing BoostRecord
    - Register button calls register_seed_boost on-chain when clicked
    - Card shows correct state: eligible (with register button), registered (waiting for stake), active (boosted), revoked
    - Card does not render for users without seed tokens and without a BoostRecord
    - Dashboard page integrates BoostStatusCard with ErrorBoundary
    - All boost-status-card tests pass: null renders for no-wallet and no-eligibility, eligible/registered/active/revoked states render correctly, button disabled while pending
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd app/web && npx tsc --noEmit`
2. useRegisterBoost behavioral tests pass: `cd app/web && npx vitest run __tests__/hooks/useRegisterBoost.test.ts --reporter=verbose`
3. BoostStatusCard render-state tests pass: `cd app/web && npx vitest run __tests__/components/boost-status-card.test.tsx --reporter=verbose`
4. All existing tests pass: `cd app/web && npx vitest run --reporter=verbose 2>&1 | tail -30`
5. Dashboard page renders without errors (visually check `BoostStatusCard` placement)
</verification>

<success_criteria>
- Users with seed tokens see a "Register Boost" button on the dashboard
- Clicking register sends `register_seed_boost` transaction via wallet
- After registration, UI shows "Registered" or "Active" based on stake state
- Users without seed tokens or boost records see no boost card
- useRegisterBoost behavioral tests verify: correct accounts, simulation before send, error cases, SPL Token program usage
- BoostStatusCard render tests verify: null renders, eligible/registered/active/revoked states, button disabled while pending
- No TypeScript errors, no test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/26-frontend-boost-ui/26-02-SUMMARY.md`
</output>
